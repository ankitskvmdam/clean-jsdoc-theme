---
title: Advanced Usage
order: 1
---

# Advanced Usage

This page lives at `docs/guides/advanced.md`, so its slug is `/guides/advanced`
and its **group is derived from the `guides/` directory** (humanized to "Guides").
This file sets a `title` and `order` in frontmatter but deliberately omits
`group`, proving the directory fallback.

## Cross-references

Markdown links resolve through the same pipeline as the API pages. A link back to
[Getting Started](/getting-started) and over to
[Frontmatter](/guides/frontmatter) both work.

## GFM still works

- [x] Tables
- [x] Task lists
- [x] Strikethrough ~~like this~~
