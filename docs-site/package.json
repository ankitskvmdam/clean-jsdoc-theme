{
  "name": "@clean-jsdoc-theme/docs-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:theme": "pnpm exec turbo run build --filter=clean-jsdoc-theme --filter=@clean-jsdoc-theme/aadesh",
    "extract": "clean-jsdoc extract",
    "build:docs": "clean-jsdoc build && jsdoc -c jsdoc.api.json",
    "docs": "pnpm run build:theme && clean-jsdoc build && jsdoc -c jsdoc.api.json",
    "dev": "pnpm run build:theme && concurrently -k -n pkgs,docs,serve \"pnpm exec turbo watch build --filter=clean-jsdoc-theme\" \"nodemon\" \"serve -l 3001 dist\"",
    "clean-node-modules": "node ../scripts/clean.mjs node_modules",
    "clean-jsdoc": "clean-jsdoc-theme",
    "i18n:prompt": "clean-jsdoc prompt"
  },
  "devDependencies": {
    "@clean-jsdoc-theme/aadesh": "workspace:^",
    "clean-jsdoc-theme": "workspace:^",
    "concurrently": "^9.0.0",
    "jsdoc": "^4.0.0",
    "nodemon": "^3.0.0",
    "serve": "^14.0.0"
  }
}
