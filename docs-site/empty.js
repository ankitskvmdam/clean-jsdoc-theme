/*
 * Intentionally empty.
 *
 * The main docs-site build (jsdoc.json) is prose-only — its content comes from
 * ./docs (Markdown). JSDoc still needs at least one .js source to scan or it
 * aborts with "no input files to process", so this file gives it one with
 * nothing to document. The API reference is a separate build that scans ./src
 * (see jsdoc.api.json).
 */
