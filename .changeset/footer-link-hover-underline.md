---
"@clean-jsdoc-theme/rang": patch
"@clean-jsdoc-theme/dwar": patch
"clean-jsdoc-theme": patch
"@clean-jsdoc-theme/typedoc": patch
---

Footer: the repository link now **underlines on hover** instead of changing color. It keeps its `text-muted-foreground` color and adds `hover:underline` (was `hover:text-accent`).
