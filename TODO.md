1. Add an option that will let the developer structure the sidebar, complete sidebar. Currently we give them the option to order the section, but a lot of users has requested a way to structure the sections items. I know this will be so hard to order all the sidebar items, but will will try to find some creative ways to do that. Maybe we can add this using aadesh that will render a cli ui and let the developer structure sidebar item by item and that we can use.
3. Add typedoc support https://github.com/JulianWowra/typedoc-github-theme/blob/main/src/GitHubTheme.tsx maybe a package like @clean-jsdoc-theme/typedoc
4. Fix the style of the entire page.
5. Work on the reaming todos in setu/docs.
6. We need to work on the release.
