/**
 * `@clean-jsdoc-theme/typedoc` showcase — a small TypeScript library that
 * exercises the **`@order`** sidebar-positioning tag on the TypeDoc path.
 *
 * The expected sidebar (see README) demonstrates `@order` ordering both the
 * parent subgroups under `Core` and the leaves within each group / section.
 *
 * @packageDocumentation
 */

export * from './core';
export * from './util';
